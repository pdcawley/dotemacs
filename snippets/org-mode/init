# -*- mode: snippet -*-
# name: init
# key: init
# --
${1:Initialization}
`(org-entry-put (point)
                "noweb-ref" (concat (org-entry-get-parent (point) "noweb-ref")
                                         "-init"))`#+BEGIN_SRC emacs-lisp :tangle no :noweb yes
  $0
#+END_SRC

