# -*- mode: snippet -*-
# name: diminish
# key: diminish
# --
${1:Diminish minor modes}
#+BEGIN_SRC emacs-lisp :tangle no :noweb yes
    ${2:`(org-entry-get-parent (point) "noweb-ref")`}
    $0
#+END_SRC`(org-entry-put (point) "noweb-ref" 
           (concat (org-entry-get-parent (point) "noweb-ref") "-diminish"))`
