# -*- mode: snippet -*-
# name: config
# key: config
# --
${1:Configuration}
`(org-entry-put (point)
                "noweb-ref" (concat (org-entry-get-parent (point) "noweb-ref")
                                         "-config"))`#+BEGIN_SRC emacs-lisp :tangle no :noweb yes
  $0
#+END_SRC

