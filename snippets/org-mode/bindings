# -*- mode: snippet -*-
# name: bindings
# key: bindings
# --
${1:Bindings}
#+BEGIN_SRC emacs-lisp :tangle no :noweb yes
  $0
#+END_SRC`(org-entry-put (point)
                "noweb-ref" (concat (org-entry-get-parent (point) "noweb-ref")
                                         "-bindings"))`
