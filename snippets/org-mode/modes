# -*- mode: snippet -*-
# name: modes
# key: modes
# --
${1:Bind modes to files}
#+BEGIN_SRC emacs-lisp :tangle no :noweb yes
    ("${2:.foo\\'}" . ${3:`(org-entry-get-parent (point) "noweb-ref")`})
#+END_SRC`(org-entry-put (point) "noweb-ref" 
           (concat (org-entry-get-parent (point) "noweb-ref") "-mode"))`
$0
