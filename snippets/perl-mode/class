# -*- mode: snippet -*-
# name: class
# --
package ${1:`(pdc:path->perl-module (buffer-file-name))`};

use Moose;
use autodie;
use namespace::sweep;
use common::sense;

$0

__PACKAGE__->meta->make_immutable;
1;